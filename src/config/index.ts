import AppConfig from './app.config';
import AuthConfig from './auth.config';
import DBConfig from './db.config';
import VideoCallConfig from './video-call.config';

export default [AppConfig, AuthConfig, DBConfig, VideoCallConfig];
